workspace = "ghostfolio/ghostfolio"

[entries.npm-cache]
tag = "ghostfolio-npm-cache"
path = "~/.npm"

[profiles.node-dependencies]
entries = ["npm-cache"]

[adapters.docker]
tag = "ghostfolio-docker"
cache-mode = "max"

[adapters.nx]
tag = "ghostfolio-nx"
profiles = ["node-dependencies"]
